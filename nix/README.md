# nix

Nix package manager configuration.

## Configuration

```bash
cd ~/.dotfiles
stow nix
```

## Settings

Enables experimental features:
- `nix-command`: New CLI interface
- `flakes`: Flake support for reproducible builds

These are required for the home-manager flake configuration.
