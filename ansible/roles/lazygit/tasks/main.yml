- name: Simulate stow lazygit
  shell:
    cmd: stow lazygit --simulate -v 
    chdir: ~/.dotfiles/
  register: sim_result
  changed_when: False

- name: Stow dotfiles
  shell:
    cmd: stow lazygit
    chdir: ~/.dotfiles
  when: '"LINK: " in sim_result.stderr'

