- name: Ensure all dnf packages are updated (Fedora)
  become: yes
  ansible.builtin.dnf:
    name: "*"
    state: latest

- name: Install dnf packages (Fedora)
  become: yes
  ansible.builtin.dnf:
    name: "{{ item }}"
    update_cache: yes
    state: latest
  loop: "{{ packages }}"
