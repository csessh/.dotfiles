- name: Simulate stow git
  shell:
    cmd: stow git --simulate -v 
    chdir: ~/.dotfiles/
  register: sim_result
  changed_when: False

- name: Stow dotfiles
  shell:
    cmd: stow git
    chdir: ~/.dotfiles
  when: '"LINK: " in sim_result.stderr'

