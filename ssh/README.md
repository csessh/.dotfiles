# SSH configuration

## Configuration

```bash
cd ~/.dotfiles
stow ssh
```

## Cloudflared

Connection to home server is routed through CloudFlare tunnel. Simple and quick.

This setup also required `cloudflared` client installed on every machine ssh to `console.tdo.io.vn` server.
